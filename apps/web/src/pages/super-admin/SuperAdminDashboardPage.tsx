import { useEffect,useState } from "react";
type Organization={id:string;name:string;slug:string;status:string;createdAt:string};
type Admin={id:string;organizationId:string;name:string;email:string;status:string;createdAt:string};
export default function SuperAdminDashboardPage(){const [orgs,setOrgs]=useState<Organization[]>([]),[admins,setAdmins]=useState<Admin[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=async()=>{const [a,o]=await Promise.all([fetch('/api/auth/admins',{credentials:'include'}),fetch('/api/super-admin/organizations',{credentials:'include'})]);if(!a.ok||!o.ok){setError('Acesso não autorizado.');setLoading(false);return}setAdmins((await a.json()).admins??[]);setOrgs((await o.json()).organizations??[]);setLoading(false)};useEffect(()=>{load()},[]);if(error)return <main className="admin-dashboard"><h1>{error}</h1></main>;return <main className="admin-dashboard"><header><div><span className="section-label">RIFA X · SUPER ADMIN</span><h1>Visão geral</h1><p>Gerencie organizações e administradores.</p></div><a href="/super-admin/organizations/new" className="primary-button">+ Organização</a></header><section className="stats"><article><span>Organizações</span><strong>{orgs.length}</strong></article><article><span>Administradores</span><strong>{admins.length}</strong></article><article><span>Ativas</span><strong>{orgs.filter(o=>o.status==='ACTIVE').length}</strong></article></section><section className="raffle-list"><div className="list-header"><h2>Organizações</h2><a href="/super-admin/organizations">Ver todas</a></div>{loading?<p>Carregando…</p>:orgs.length===0?<div className="empty"><strong>Nenhuma organização</strong><p>Crie a primeira organização.</p></div>:orgs.map(o=><a className="raffle-row" href={`/super-admin/organizations/${o.id}`} key={o.id}><div><strong>{o.name}</strong><small>/{o.slug}</small></div><span className={`status status-${o.status.toLowerCase()}`}>{o.status}</span><b>›</b></a>)}</section><section className="raffle-list"><div className="list-header"><h2>Administradores</h2><a href="/super-admin/admins">Gerenciar</a></div>{admins.map(a=><div className="raffle-row" key={a.id}><div><strong>{a.name}</strong><small>{a.email}</small></div><span className="status status-active">ATIVO</span></div>)}</section></main>}
