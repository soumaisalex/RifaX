import { FormEvent,useEffect,useState } from "react";
export default function OrganizationFormPage(){const [orgs,setOrgs]=useState<{id:string;name:string;slug:string}[]>([]),[form,setForm]=useState({name:"",slug:""}),[admin,setAdmin]=useState({organizationId:"",name:"",email:"",password:""}),[message,setMessage]=useState(""),[saving,setSaving]=useState(false);useEffect(()=>{fetch('/api/super-admin/organizations',{credentials:'include'}).then(r=>r.json()).then(d=>setOrgs(d.organizations??[]))},[]);const submit=async(e:FormEvent)=>{e.preventDefault();setSaving(true);setMessage("");const r=await fetch('/api/super-admin/organizations',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify(form)});if(!r.ok){setMessage((await r.json()).error??'Erro');setSaving(false);return}const o=await r.json();setOrgs(x=>[...x,o.organization]);setAdmin(a=>({...a,organizationId:o.organization.id}));setMessage('Organização criada. Agora você pode criar o administrador.');setSaving(false)};const createAdmin=async(e:FormEvent)=>{e.preventDefault();setSaving(true);const r=await fetch('/api/auth/admins',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify(admin)});setMessage(r.ok?'Administrador criado com sucesso.':((await r.json()).error??'Erro'));setSaving(false)};return <main className="admin-form"><header><span className="section-label">RIFA X · SUPER ADMIN</span><h1>Nova organização</h1><p>Crie uma organização e seu administrador.</p></header><form onSubmit={submit}><section><h2>Organização</h2><label>Nome<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Slug<input required value={form.slug} onChange={e=>setForm({...form,slug:e.target.value.toLowerCase().replace(/\s+/g,'-')})}/></label><button disabled={saving}>Criar organização</button></section></form><form onSubmit={createAdmin}><section><h2>Administrador</h2><label>Organização<select required value={admin.organizationId} onChange={e=>setAdmin({...admin,organizationId:e.target.value})}><option value="">Selecione</option>{orgs.map(o=><option value={o.id} key={o.id}>{o.name}</option>)}</select></label><label>Nome<input required value={admin.name} onChange={e=>setAdmin({...admin,name:e.target.value})}/></label><label>E-mail<input required type="email" value={admin.email} onChange={e=>setAdmin({...admin,email:e.target.value})}/></label><label>Senha<input required minLength={8} type="password" value={admin.password} onChange={e=>setAdmin({...admin,password:e.target.value})}/></label><button disabled={saving}>Criar administrador</button></section></form>{message&&<p role="status">{message}</p>}</main>}
